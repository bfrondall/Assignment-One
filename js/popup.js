
//This is failed popup code that I tried to make work. My goal was to make it so you could click the button and have the gif overlay on the sight, 
//however, I could not figure out how to make the button work with the gif


//console.log("Im the popup!")

//$('#mybutton').on('Click', changeTheme);

//function changeTheme() {
	//var url = $(this).attr('id');

	//chrome.tabs.query({active: true, currentWindow: true}, function(tabs) {
	//chrome.tabs.sendMessage(tabs[0].id, {message: url});
	//});
//}

//$('#mybutton').on('Click', function(){
	//$('body').addClass(".noise")
//})